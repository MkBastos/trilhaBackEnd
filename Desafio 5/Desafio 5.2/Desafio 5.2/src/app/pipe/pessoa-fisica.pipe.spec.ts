import { PessoaFisicaPipe } from './pessoa-fisica.pipe';

describe('PessoaFisicaPipe', () => {
  it('create an instance', () => {
    const pipe = new PessoaFisicaPipe();
    expect(pipe).toBeTruthy();
  });
});
